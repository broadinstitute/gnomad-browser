query GeneViewer($geneName: String!) {
  gene(gene_name: $geneName) {
    id: gene_name
    gene_name
    transcript {
      exons {
        feature_type
        start
        stop
        strand
      }
    }
  }
}
