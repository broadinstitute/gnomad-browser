{
  gene(gene_id: "ENSG00000186951") {
    gene_id
    gene_name
    start
    stop
    exome_variants {
      chrom
      pos
      ref
      alt
      variant_id
      allele_num
      allele_freq
      allele_count
    }
    exome_coverage {
      pos
      mean
    }
    genome_variants {
      chrom
      pos
      ref
      alt
      variant_id
      allele_num
      allele_freq
      allele_count
    }
    exome_coverage {
      pos
      mean
    }
    transcript {
      exons {
        feature_type
        start
        stop
      }
    }
  }
}
